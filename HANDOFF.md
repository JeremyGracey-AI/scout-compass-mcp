# HANDOFF.md — Scout Compass build handoff

**Audience:** Claude Code, continuing this project with Jeremy Gracey.
**Repo root:** `~/GitHub/scout-compass` (moved here from `~/Downloads/files 5/` on June 12 and git-initialized). Read this fully before touching code.

---

## Mission and hard deadline

Scout Compass is a hackathon entry for **Agents League @ AI Skills Fest 2026, Reasoning Agents track (Microsoft Foundry)**.

- **Submission deadline: June 14, 2026, 11:59 PM Pacific. Target: submit June 13 night.** If a task isn't visible in the demo video or required by the rules, do not build it.
- Submission goes through the Contest Website "Projects" tab (NOT a GitHub issue): project description, demo video ≤5 min (YouTube/Vimeo, solely Jeremy's own work, no third-party music/stock), **public GitHub repo**, **architecture diagram** (`docs/architecture.svg`), Microsoft Learn username.
- Judging: 20% accuracy, 20% reasoning/multi-step, 15% creativity, 15% UX, 20% reliability/safety, 10% Discord vote. README has an explicit rubric-mapping section — keep it true.

## The one-sentence thesis (protect this in every change)

An agent's entire memory — skills, knowledge, and a blackbox of decision records — lives as plain Markdown in a git repo the human owns; **the agent's only write paths are decisions and proposals, and promotion to active memory requires the human-gated `approve_proposal`** ("agents propose, humans promote"); `git revert` is memory rollback — for skills and knowledge only. **Behavior is revertible; history is not:** `revert_memory` refuses `[blackbox]` commits, so the flight recorder is append-only even for humans.

Enforced in `server/src/tools.ts` (no tool writes `skills/` or `knowledge/` directly; the revert guard checks the commit's `[actor]` prefix). Never weaken either, even as a convenience.

## Current state: DONE and verified (June 12)

Everything below compiles, and `demo/smoke-test.mjs` passes all checks (exits 1 on any failure — it is a real test now, the old step-6 check was vacuous):

| Area | Status | Notes |
|---|---|---|
| Vault layer + **slug-named promotion** (`skill-triage-invoice-inv-7731-initech`, deterministic, collision-guarded) | ✅ | `server/src/vault.ts` |
| Git layer + **vault-wide write lock** (stateless HTTP = concurrent requests; all mutating tools serialize) + `subject()` for the revert guard | ✅ | `server/src/git.ts` |
| Audit heuristics ×3; **heuristic 1 now re-runs recall over the uncited decision's task and the proposal cites the existing notes the agent overlooked** (`overlooked:` frontmatter + "What the agent missed" section). The draft procedure derives from real vault content — closes the "where did the correct skill come from?" hole | ✅ | `server/src/audit.ts` |
| MCP tools ×9 + **blackbox revert refusal** + **server-verified citations** (unresolved ids flagged `citations_unresolved`, never dropped) | ✅ | `server/src/tools.ts` |
| Entrypoint (stdio + streamable HTTP `:3000/mcp`, `/healthz`) — verified over HTTP: 9 tools listed, refusal message confirmed through the real MCP surface | ✅ | `server/src/index.ts` |
| Seed: 2 skills, 4 knowledge, **3 prior decisions** — dec-003 (low-confidence, cited) was added so **all three heuristics fire in the on-camera audit** | ✅ | `demo/seed-vault.mjs` |
| Smoke test: recall → freelance → audit (3 findings) → proposal cites kn-payment-policy → approve (slug) → recall ranks new skill #1 → blackbox revert refused → approval revert verified | ✅ | `demo/smoke-test.mjs` |
| Trap email + expected runs (updated: 3 findings, slug name, optional blackbox-refusal beat) | ✅ | `demo/trap-email.md` |
| Atlas instructions | ✅ | `agent/atlas-instructions.md` |
| README (rubric mapping added), one-pager, architecture SVG, spec | ✅ | `README.md`, `docs/` |
| Dockerfile (no longer copies `vault/` — the seed script generates it) | ✅ | `server/Dockerfile` |

**Packaging decision (important):** `vault/` is **gitignored**, not committed. It contains its own inner `.git`, and git records nested repos as empty gitlinks — a fresh clone would have gotten an empty vault silently. The vault is fully regenerated by `node demo/seed-vault.mjs`; README quickstart says so.

Verify before starting any work:

```bash
cd server && npm install && npm run build
node ../demo/seed-vault.mjs        # regenerates vault/ deterministically (required after clone)
node ../demo/smoke-test.mjs        # must print "SMOKE TEST COMPLETE — all checks PASS", exit 0
MODE=http npm start                # then: curl localhost:3000/healthz
```

## Remaining work, in strict priority order

### P0 — Expose the server (June 12) — **dev tunnel is the PRIMARY path, not the fallback**
Decision already made (we lost a day; the original plan's "today" was June 11). A demo video needs a reachable HTTPS URL for a few hours, nothing more — Container Apps buys zero rubric points. Jeremy must run (interactive auth, type `!` before the command in Claude Code to run it in-session):

```bash
# 1. Install devtunnel (Claude's sandbox can't pipe-to-shell):
! curl -sL https://aka.ms/DevTunnelCliInstall | bash
# 2. Login (Microsoft account used for Foundry):
! devtunnel user login
# 3. Start server + tunnel:
cd ~/GitHub/scout-compass/server && MODE=http npm start &
devtunnel host -p 3000 --allow-anonymous
```

Acceptance: `curl https://<tunnel-host>/healthz` returns ok; `tools/list` over POST `/mcp` returns 9 tools (send `Accept: application/json, text/event-stream`).
Container Apps (`server/Dockerfile`, env `MODE=http PORT=3000 VAULT_PATH=/app/vault`) only if everything else is done early. Container filesystem is ephemeral — fine for the demo; do NOT engineer persistence.

### P0 — Wire the Foundry agent (June 12)
1. Foundry Agent Service agent **Atlas**, GPT-4o-class deployment, remote MCP tool → `https://<tunnel-host>/mcp`, instructions verbatim from `agent/atlas-instructions.md`.
2. **Highest-risk behavior:** Atlas must log honestly-empty citations when it freelances (run 1). Test the trap email ≥3 times. If Atlas pads citations, tighten instruction #3 ("citing a note you did not actually retrieve and follow is a contract violation worse than an empty list") — iterate on instructions, NOT server code. Note: padded citations now also show up as `citations_unresolved` if the ids are fake — that's the audit story working, mention it rather than fighting it.
3. Acceptance run 1 (fresh seed): freelance → `citations: []`, confidence ≲0.6 → human asks for audit → **three findings**, proposal in `proposed/` whose "What the agent missed" cites `kn-payment-policy`.
4. Acceptance run 2 (after approve): same email → recall ranks the slug-named skill #1 → Atlas follows it → `needs_human`, cites the skill.
5. **`run_audit` is human-triggered** (operator asks; agent relays). The agent never audits itself unprompted — that's part of the governance story, say it on camera.
6. Track-rules check (5 min): confirm the Reasoning Agents track doesn't require Foundry *Agent Service* specifically before relying on the thin-orchestrator fallback (see "If something breaks").

### P1 — Demo recording (June 13)
7-step arc per `docs/scout-compass-spec.md` §6, plus two new beats that are now free:
- The audit shows **three** heuristics, not one — pan over the report.
- Optional: ask Atlas to revert the run-1 decision commit → server refuses on camera ("behavior is revertible, history is not").
Layout: agent chat left, Obsidian on `vault/` right (test graph view vs. file-explorer+open-note beforehand — graph reflow can be janky; pick whichever reads better), terminal with `git log --oneline` bottom. Reseed before every take. **Record in segments** — don't bet the night on one take; budget for the run-2 segment needing several attempts. ≤5 min hard rule; the current script runs ~3.5, let the audit→approve→re-run beats breathe to ~4:15.

### P1 — Submission package (June 13, before recording fatigue)
Repo public on GitHub (`gh repo create scout-compass --public --source . --push`), video uploaded, description written from README, architecture diagram attached, Microsoft Learn username. Submit by June 13 night = 24h buffer.

### P2 — Only if everything above is done
- **Discord post** (10% of score — cheapest points on the board, do this before any stretch): short pitch + repo link + 30s clip in the Agents League Discord.
- **Foundry IQ stretch** ($5K side award): second retrieval backend inside `recall_knowledge`, vault results tagged `source: vault`. Abort if >3 hours or destabilizing.
- Copilot Studio second entry. Entries: 3/challenge, 9 total; one prize per person.

## Conventions and guardrails (do not violate)

- **Stack frozen:** TypeScript, MCP SDK 1.29 (`registerTool`), simple-git, gray-matter, zod, express. No new runtime deps; no databases; no embeddings. The vault IS the database.
- ESM throughout: imports use `.js` suffixes; never `require()`.
- Every vault mutation = a git commit with the `[actor] message` convention, **inside `withVaultLock`** (see `git.ts`). Any new write path must do both.
- Frontmatter contracts (ids `dec-NNN`, `prop-NNN`, `skill-*`, `kn-*`; `overlooked:`, `citations_unresolved:`) are load-bearing for `audit.ts`, the smoke test, and the demo. Change only with matching seed + smoke-test updates.
- Don't "improve" the trap email — its two failure conditions (net-60 + unknown vendor) are calibrated. Don't touch seeded cite_counts: `skill-meeting-summary` at 0 and `dec-003` at confidence 0.5 exist so heuristics 2 and 3 fire on camera.
- Honest-citation behavior is sacred: never fix empty citations server-side; the audit catching them IS the product. (Flagging unresolved ids is detection, not correction — that stays.)
- **Re-run `node demo/smoke-test.mjs` after ANY server change. It exits 1 on failure now — trust the exit code.**
- Naming: "a compass *for* the Scout era" — never imply Microsoft affiliation.

## Facts for any materials you generate

- Builder: Jeremy Gracey, solo. jeremygracey.ai · github.com/JeremyGracey-AI · Jeremy.a.gracey@gmail.com.
- Positioning: Microsoft governs agent *permissions*; Scout Compass governs agent *memory and competence*.
- Healthcare/regulated industries = one closing slide only. Do not make the demo healthcare-specific.

## If something breaks

1. `node demo/smoke-test.mjs` isolates server vs. agent vs. deployment.
2. Foundry MCP failing → client must send `Accept: application/json, text/event-stream`; server is stateless (`sessionIdGenerator: undefined`).
3. Total Foundry meltdown (June 13 only): record with the stdio server + Claude Desktop as the agent; present Foundry wiring in the diagram + repo; note honestly in the README which client appears in the video. A working governed loop on video beats a broken integration.

**Definition of done:** submitted on the Contest Website by June 13 night with repo public, video live, diagram attached, and the smoke test passing on a fresh clone (`npm install && npm run build && node ../demo/seed-vault.mjs && node ../demo/smoke-test.mjs`).
